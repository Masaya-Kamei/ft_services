apiVersion: 1

providers:
- name: 'dashboard'
  orgId: 1
  folder: ''
  type: file
  disableDeletion: true
  editable: true
  options:
  # dashboardsのjsonファイルを置くpath
  # jsonファイルの uid は一意である必要がある
    path: /tmp/grafana/data/dashboards
